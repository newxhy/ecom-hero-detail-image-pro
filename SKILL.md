---
name: ecom-hero-detail-image-pro
version: 2.0.0
display_name: 跨类目电商主图+详情页 AI 作图（通用版 · 千问办公专用）
display_name_en: Cross-category E-commerce Hero & Detail Image Skill (QwenWork only)
description: |
  跨类目通用的电商主图 + 详情页 AI 作图技能（仅限千问办公 QwenWork 使用）。
  给任意品类的产品图 + 基本卖点，按"买家决策顺序"规划页面任务，
  产出 HTML 版式稿 + 逐屏生图提示词（阶段A），再用图像模型批量出成品（阶段B）。
  适用淘宝/天猫/京东/拼多多/亚马逊等，任何品类。
  阶段B 依赖千问办公内置的 qwenwork_image_generate，WorkBuddy 等其他平台跑不通。
  当用户说「做主图/详情页/卖点图/电商生图/给这个产品出一套图」时使用。
description_en: |
  A cross-category e-commerce hero-image + detail-page AI image skill, for QwenWork only.
  Given any product's photos and basic selling points, it plans page tasks by buyer decision
  order, outputs an HTML layout draft + per-screen image prompts (Stage A), then batch-generates
  finals with an image model (Stage B). Works for any category on Taobao / Tmall / JD / PDD /
  Amazon. Stage B depends on QwenWork's built-in qwenwork_image_generate and will not run on
  other agent platforms such as WorkBuddy.
license: MIT
compatibility: 仅限千问办公（QwenWork）。阶段A（版式稿+提示词）为纯指令，任何平台都能跑；阶段B（批量出图）强依赖千问办公内置的 qwenwork_image_generate 工具（edit 模式 + open-image-2 / banana-image-2 模型）及其并发/水印/安全拦截等具体行为，WorkBuddy 等其他 Agent 平台无该工具，阶段B 跑不通。真人/儿童照片不走图像模型（安全会拒），本地排版嵌入。
platform: 千问办公 QwenWork（专用）
platform_note: 阶段B 依赖 qwenwork_image_generate；移植到其他平台前必须先把该工具换成对方等价能力，否则只保留阶段A。
agent_created: true
---

# 跨类目电商主图 + 详情页 AI 作图（通用版）

> **定位**：这是一套"做一整套电商图"的方法，不针对任何具体品类。任何产品（护肤/家居/3C/食品/日用…）都走同一流程：先规划，再出图。
> **给谁用**：不会代码/不会 API/不科学上网的运营、设计、卖家本人。
> **两阶段交付**：阶段A = 文案结构 + HTML 版式稿 + 逐屏生图提示词（先给你确认）；阶段B = 用图像模型批量出成品。

> ⚠️ **运行环境：本技能是千问办公（QwenWork）专用，不是跨平台通用技能。**
> 第四~六节的出图纪律全部是按**千问办公内置 `qwenwork_image_generate`**（模型 open-image-2 / banana-image-2）的实测行为写的：edit 模式参考图必须在会话工作目录、并发上限约 4、竖幅出图靠底部留白带裁掉"AI生成"水印、open-image-2 对婴儿形象硬拦截、方圆体字重用本机 Pillow 拉。
> 换到 **WorkBuddy** 等其他 Agent 平台，没有这个工具、模型行为也不同，**阶段B（批量出图）直接跑不通**；只有阶段A（规划 + HTML 版式稿 + 逐屏提示词）是纯文本产出，可以照搬。移植前请先确认对方的生图能力并相应改写第四~六节。

---

## 一、核心心法：做"一张图" vs 做"一套图"

做一张图只解决"好不好看"；做一套图要同时解决三件事，缺一套就散：
1. **规格**：平台硬指标（尺寸、白底纯度、产品占比、格式、体积、详情切片高度）。不满足，再好看也上不了架。
2. **张数**：图位有固定含义（第 1 张干嘛、第 5 张干嘛），不是多生几张随便挑。
3. **连贯**：整套摆一起要像"一个人做的"——同一套配色/字体/光位/风格。这条最贵、最易被跳过。

**贯穿公式（跨类目通用）**：
`买家需求排序 → 产品卖点匹配 → 页面任务分配 → 场景证据表达 → 风格系统锁定 → 产品一致性锁定 → 批量生成`
前四步决定**说服力**，后两步决定**能不能一晚出十套、且套套不乱**。

---

## 二、怎么用（先问清，再规划，再出图）

**第 0 步 · 收素材（决定方向准不准）**
- **本品图**：真实拍摄 / 官方白底图（最重要，AI 不重绘产品）。来源优先级：① 自有实拍/官方图 → ② 品牌方天猫详情页抓（登录态浏览器打开 `detail.tmall.com/item.htm?id=…`，JS 提取 `img.alicdn.com/imgextra/<卖家ID>/…` 或 `bao/uploaded/…` 图 URL，详情长图先滚动懒加载再下载）→ ③ 现有图兜底。
- **竞品图**：2–3 家同品类做得好的主图+详情页（定风格、抄可测量指标：暗角/留白/占比，不抄文案）。
- **平台**：淘宝/天猫/京东/拼多多/亚马逊（尺寸与红线不同）。

**第 1 步 · 选交付模式**
- 模式 A：只出提示词 + HTML 版式稿，你自己拿去出图。
- 模式 B：我陪你走完，平台有内置生图就直接出成品。
→ 没说清先问一句："只拿提示词 HTML，还是我直接帮你出完？"

**第 2 步 · 先当买家，列 8 条内心疑问（跨类目找卖点的关键）**
把自己当第一次见这个产品的买家，列出下单前的 8 条顾虑（例：这到底是什么/成分材质安全吗/能用多久/好看吗/送人体面吗/每个角度顺眼吗/多大值不值/我什么场景会用）。
**问完，页面任务自动浮现**——一套详情页不是按"卖点列表"排，而是按"买家决策顺序"排：**每一屏消除一个顾虑，同时增加一个购买理由**。

**第 3 步 · 出方案（阶段A）**
给你：① 每屏写什么（一屏一个任务）② HTML 版式稿（版式/放哪/谁做，**完整生图提示词内嵌进每屏卡片，别只写"见 xx.md"**）③ 逐屏中文提示词。
**出提示词时就要按"发挥 AI 强项"设计（60/80 分思维）**：满足需求=60 分；再用 AI 的场景/氛围/融合/构图能力做到更好=80 分。凡 PS 也能拼的"平铺网格/白底堆字/单品居中"只算 60 分，提示词要重写往 80 分。**但 80 分有硬底线：绝不能以"改产品"为代价（见第四节）。**

**第 4 步 · 出图（阶段B，分层跑，别一把梭）**
- **先干跑**：列任务数与预估成本；**再试跑 1 个图位**确认版式；满意才放开全量。别一上来把几十张全丢进去赌提示词写对了。
- **每个图位出 3–4 张候选挑 1 张定稿**（AI 出图有失败率：文字糊/构图歪）。
- 图像操作（抠图/合成/换背景/边缘融合/去水印/放大）一律交给图像模型（edit 模式，以真产品图为参考）。

---

## 三、主图 vs 详情页：两套评判标准

- **主图负责"点击"**：干净、突出、3 秒读懂。亚马逊主图零瑕疵合规（纯白底、不创意）；国内主图允许带少量卖点但要防"牛皮癣"。
- **详情页负责"转化"**：按买家决策顺序讲服。
- **牛皮癣（国内红线）**：图上乱贴促销（限时特惠/价格标签/买一送一爆炸块/边框水印）会被系统识别→不给流量扶持甚至降权。**主图干净本身就是竞争力**。促销交给平台官方工具渲染，别画进图。

**平台规格速查**

| 平台 | 主图 | 详情页宽 | 备注 |
|---|---|---|---|
| 淘宝/天猫 | 800×800 | 750 或 790 | 文字占图 ≤20% |
| 京东 | 800×800 | 750 | |
| 拼多多 | 800×800 | 750 | |
| 亚马逊 | 1000×1000+ 纯白底 | 1000+ | 主图不创意、辅图≤9 |

**详情长图要切片**：别传一张 8000px 长图（加载白屏、跳失高），切成 **1200–1500px 一张**最稳。

---

## 四、出图纪律（跨类目通用，防止翻车）

1. **产品一致性锁定（最高优先）**：产品本体（外形/logo/颜色/结构）必须用真实图，不能让模型重绘——它会改形状、改颜色、画出不存在的零件 = 卖假货。多色/配色屏尤其：颜色准确是底线，模型重绘会把颜色画偏；要好看就在真实产品图基础上做排布，不靠模型重画颜色。
2. **一切图像操作用图像模型，别手动抠图**：抠图/合成/换底/边缘融合/去水印/放大都交给图像模型（手抠边缘生硬、融不进背景）。把真实产品图放到彩色背景上时，用模型或语义抠图去掉原白底，别留白方块。
3. **部件/结构屏用"真图当参考 + 模型生成一致场景"**：杯盖/内胆分层/按键这类，(a) 不给参考瞎画=改产品；(b) 真照片直接贴白底=丑。正解：把真零件图喂给模型、要求"严格保持形状/颜色/结构不变"，让它生成带引线标注的立体分解/场景图。尺寸/参数屏同理，让产品融入使用场景再标尺寸，别白底单品。
4. **真人/儿童形象看模型**：千问办公内置 open-image-2 对婴儿形象硬拦截（生成/编辑都拒），豆包等可生成。真人生活照一律**本地原样嵌入相框 + 排版**，不送图像模型重绘脸。
5. **文字按元素拆**：大号标题/副标题/胶囊模型能逐字画对；密集小字、精确数字、备案号、参数表模型必乱码——这些留白后期本地贴字。**有真实信息（说明书/现有详情里的参数、尺寸、成分名）就如实填，别滥用留白；但别为加文字把已好看的图降级成白底简版。**
6. **模型自带水印用"留白带"去掉**：竖幅出图、内容压上方约 56–68% 方形安全区、下方留空白带，水印落进空白带，裁掉即可（内容压不够高会被裁，宁可多留白）。
7. **红线通用**：不画按钮/仿 UI、不上价格促销、不自造盾牌/奖章/榜单徽章、绝对化用语别说、竞品品牌名别出现、备案号批准文号别用 AI 画。

---

## 五、详情页 12 段动线骨架（跨类目通用模板）

按买家决策顺序排（国内先扎痛点再给方案；跨境可先参数后场景）：
`头图海报 → 痛点唤起 → 核心卖点 → 产品展示 → 细节特写 → 参数表格 → 使用场景 → 对比佐证 → 资质检测 → 品牌实力 → 服务物流 → 售后保障`
每屏对应消除一个顾虑。可按品类增删，但顺序不乱。

**风格系统锁定（连贯的关键）**：开工前定死一套——主色/辅色/底色、标题字体（圆体/黑体等）、正文字体、光位氛围、标题句式与字数、人群口径。整套屏共用，换品类只换值不换规则。

---

## 六、出图后自查

- 产品是真实图、颜色/结构没被改 ✓
- 一套图风格连贯（同配色/字体/光位）✓
- 每屏一个买家任务、按决策顺序 ✓
- 无按钮/价格/促销/自造徽章/牛皮癣 ✓
- 密集小字与精确数字无乱码（该留白的留白、该填的填真值）✓
- 模型水印已去、边缘自然融合无剪贴白边 ✓
- 规格达标（尺寸/占比/体积/详情切片 1200–1500px）✓
- 真人/儿童照为本地嵌入、未送模型重绘 ✓
